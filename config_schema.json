{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "region": "动画",           // 可选：分区名称或rid，不配置则全站随机
    "send_type": "image",       // link 或 image
    "block_keywords": ["政治", "低俗"],  // 敏感词列表（可选）
    "show_processing": false     // 是否显示“正在搬运”提示（避免刷屏）
}
    
  }
